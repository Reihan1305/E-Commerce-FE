import { Box } from "@mui/material";
import FooterItem from "./FooterItem";

const Footer = () => {
    return (
        <Box
            sx={{
                marginTop: "auto"
            }}
        >
            <FooterItem />
        </Box>
    );
};

export default Footer;
