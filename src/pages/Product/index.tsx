import { Box } from '@mui/material';
import ProductList from './productList';

const Product = () => {
    return (
        <Box sx={{ p: 3 }}>
            <ProductList />
        </Box>
    );
};

export default Product;
